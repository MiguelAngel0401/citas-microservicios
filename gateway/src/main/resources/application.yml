server:
  port: 8090

spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: mcv-medicos
              uri: lb://mcv-medicos
              predicates:
                - Path=/api/medicos/**
              filters:
                - StripPrefix=2

            - id: mcv-citas
              uri: lb://mcv-citas
              predicates:
                - Path=/api/citas/**
              filters:
                - StripPrefix=2

    loadbalancer:
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka